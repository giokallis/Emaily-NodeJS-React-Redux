{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server/src/index.js",
  "scripts": {
    "server": "npm install && npm run dev --prefix server",
    "client": "npm install && npm run start --prefix client",
    "start": "cd server && npm install && npm run dev",
    "dev": "concurrently \"npm run server \" \"npm run client\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "2.2.1"
  },
  "dependencies": {
    "concurrently": "^6.0.0"
  }
}
